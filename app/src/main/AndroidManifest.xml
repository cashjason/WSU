<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cashj.wsu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home"
            android:label="Home"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".PostPracticeEval"
            android:label="Pregame Evaluation"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".History"
            android:label="History"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".CreateAccount"
            android:label="Create Account"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".PostgameEval"
            android:label="Postgame Evaluation"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".ForgotPassword"
            android:label="Forgot Password"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".Evaluations"
            android:label="Evaluations"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".Profile"
            android:label="Profile"
            android:theme="@style/AppTheme">
        </activity>
    </application>

</manifest>